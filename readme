# Ads Remove Extension 🚫

**Ads Remove** คือ Chrome Extension ที่พัฒนาขึ้นเพื่อลบ Element โฆษณาหรือส่วนเกินที่ไม่ต้องการออกจากหน้าเว็บไซต์โดยอัตโนมัติ ช่วยให้หน้าเว็บสะอาดตาและโหลดได้ลื่นไหลยิ่งขึ้น

## ✨ ความสามารถหลัก (Features)

* **Auto Remove:** ลบโฆษณาอัตโนมัติทันทีที่เข้าหน้าเว็บ
* **Dynamic Blocking:** ทำงานร่วมกับ `MutationObserver` ทำให้สามารถลบโฆษณาที่เพิ่งโหลดขึ้นมาทีหลัง (เช่น พวก Lazy load ads) ได้
* **Toggle ON/OFF:** มีสวิตช์เปิด-ปิดการทำงานได้ง่ายๆ ผ่าน Popup
    * **เปิดใช้งาน (Turn ON):** เริ่มลบโฆษณาทันทีโดยไม่ต้อง Reload หน้า
    * **ปิดใช้งาน (Turn OFF):** หยุดการทำงานและ Reload หน้าเว็บเพื่อให้เนื้อหากลับมาเหมือนเดิม
* **Customizable:** สามารถเพิ่ม Class หรือ ID ของโฆษณาที่ต้องการบล็อกเพิ่มได้ง่ายๆ ในโค้ด

## 📦 โครงสร้างไฟล์ (File Structure)

* `manifest.json`: การตั้งค่าหลักของ Extension (Manifest V3)
* `popup.html` / `popup.js`: ส่วนแสดงผลหน้าต่างเล็กๆ เมื่อกดไอคอน Extension
* `main.js`: ตัวจัดการหลัก คอยสั่งงาน Script ลบโฆษณาเมื่อมีการเปลี่ยนแปลงในหน้าเว็บ
* `script/removeAds.js`: ฟังก์ชันสำหรับค้นหาและลบ Element
* `script/adsList.js`: รายชื่อ Keyword (Class/ID) ที่ต้องการจะลบ

## 🚀 วิธีการติดตั้ง (Installation)

เนื่องจาก Extension นี้ยังไม่ได้อยู่บน Chrome Web Store ต้องติดตั้งแบบ **Developer Mode** ดังนี้:

1.  ดาวน์โหลดหรือ Clone โปรเจกต์นี้ลงในเครื่องคอมพิวเตอร์
2.  เปิด Google Chrome และไปที่ URL: `chrome://extensions/`
3.  เปิดสวิตช์ **"Developer mode"** (โหมดนักพัฒนาซอฟต์แวร์) ที่มุมขวาบน
4.  คลิกปุ่ม **"Load unpacked"** (โหลดส่วนขยายที่คลายการบีบอัดแล้ว)
5.  เลือกโฟลเดอร์ของโปรเจกต์นี้

## 📖 วิธีการใช้งาน (Usage)

1.  คลิกที่ไอคอน **Ads Remove** บนแถบ Extension ของ Chrome
2.  จะปรากฏหน้าต่าง Popup แสดงสถานะปัจจุบัน:
    * 🟢 **Status ON:** Extension กำลังทำงาน (ปุ่มจะเป็นสีแดงเพื่อให้กดปิด)
    * 🔴 **Status OFF:** Extension ปิดอยู่ (ปุ่มจะเป็นสีเขียวเพื่อให้กดเปิด)
3.  **การปรับแต่งรายการโฆษณา:**
    หากพบโฆษณาที่ยังไม่ถูกลบ สามารถเข้าไปเพิ่ม Selector ได้ที่ไฟล์ `script/adsList.js`
    ```javascript
    const adsList = [
        '[class*="center_"]',
        '[id*="sticky-bottom"]',
        // เพิ่ม Class หรือ ID ที่ต้องการที่นี่
    ]
    ```

## ⚠️ หมายเหตุ
Extension นี้สร้างขึ้นเพื่อการเรียนรู้และการใช้งานส่วนตัว อาจไม่สามารถบล็อกโฆษณาได้ 100% ในทุกเว็บไซต์ ขึ้นอยู่กับโครงสร้างของเว็บไซต์นั้นๆ

---
Created for personal use.